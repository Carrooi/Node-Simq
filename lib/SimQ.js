// Generated by CoffeeScript 1.6.3
(function() {
  var Configurator, Loader, Parser, SimQ, fs, ncp, watch, _path;

  fs = require('fs');

  watch = require('watch');

  _path = require('path');

  ncp = require('ncp');

  Loader = require('./Loader');

  Parser = require('./Parser');

  Configurator = require('./Config/Configurator');

  SimQ = (function() {
    SimQ.prototype.basePath = '.';

    SimQ.prototype.configPath = 'config/setup.json';

    SimQ.prototype.config = null;

    SimQ.prototype.debug = false;

    SimQ.prototype.parser = null;

    function SimQ(debug, configPath) {
      this.debug = debug;
      if (configPath == null) {
        configPath = null;
      }
      if (!configPath) {
        configPath = this.configPath;
      }
      this.configPath = configPath;
      this.config = new Configurator(this.basePath + '/' + this.configPath, this.debug);
      this.parser = new Parser(this, new Loader(this), this.basePath);
    }

    SimQ.prototype.build = function() {
      var config, name, pckg, _ref,
        _this = this;
      config = this.config.load();
      _ref = config.packages;
      for (name in _ref) {
        pckg = _ref[name];
        if (pckg.application) {
          fs.writeFileSync(this.basePath + '/' + pckg.application, this.parser.parseApplication(pckg, !config["debugger"].scripts || !this.debug));
        }
        if (pckg.style && pckg.style["in"] && pckg.style.out) {
          (function(pckg) {
            return _this.parser.parseStyles(pckg.style["in"], !config["debugger"].styles || !_this.debug, function(content) {
              return fs.writeFileSync(_this.basePath + '/' + pckg.style.out, content);
            });
          })(pckg);
        }
      }
      return this;
    };

    SimQ.prototype.watch = function() {
      var _this = this;
      this.build();
      watch.watchTree(this.basePath, {
        persistent: true,
        interval: 1000
      }, function(file, curr, prev) {
        if (curr && (curr.nlink === 0 || +curr.mtime !== +(prev != null ? prev.mtime : void 0))) {
          return _this.build();
        }
      });
      return this;
    };

    SimQ.create = function(name) {
      var path;
      if (!name) {
        throw new Error('Please enter name of new application.');
      }
      path = _path.resolve(name);
      if (fs.existsSync(path)) {
        throw new Error('Directory with ' + name + ' name is already exists.');
      }
      return ncp.ncp(_path.normalize(__dirname + '/../sandbox'), path, function(err) {
        if (err) {
          throw new Error('There is some error with creating new application.');
        }
      });
    };

    SimQ.prototype.getModuleName = function(path) {
      path = _path.normalize(path);
      return path.replace(new RegExp(_path.extname(path) + '$'), '');
    };

    return SimQ;

  })();

  module.exports = SimQ;

}).call(this);
