// Generated by CoffeeScript 1.6.3
(function() {
  var Configurator, Parser, Q, SimQ, express, fs, mime, ncp, watch, _path;

  fs = require('fs');

  watch = require('watch');

  _path = require('path');

  ncp = require('ncp');

  express = require('express');

  Q = require('q');

  mime = require('mime');

  Parser = require('./Parser/Parser');

  Configurator = require('./Config/Configurator');

  SimQ = (function() {
    SimQ.prototype.v = false;

    SimQ.prototype.basePath = '.';

    SimQ.prototype.configPath = 'config/setup.json';

    SimQ.prototype.config = null;

    SimQ.prototype.debug = false;

    SimQ.prototype.parser = null;

    function SimQ(debug, basePath, configPath) {
      this.debug = debug != null ? debug : false;
      this.basePath = basePath != null ? basePath : '.';
      if (configPath == null) {
        configPath = null;
      }
      if (!configPath) {
        configPath = this.configPath;
      }
      this.configPath = _path.resolve(this.basePath + '/' + configPath);
      this.config = new Configurator(this.configPath, this.debug);
      this.parser = new Parser(this, this.basePath);
    }

    SimQ.prototype.server = function() {
      var app, base, config, data, main, name, path, pckg, route, _fn, _ref, _ref1,
        _this = this;
      app = express();
      config = this.config.load();
      base = config.routes.prefix === null ? '/' : '/' + config.routes.prefix;
      main = _path.resolve(this.basePath + '/' + config.routes.main);
      if (fs.existsSync(main)) {
        if (this.v) {
          console.log("Mapping file '" + main + "' to '" + base + "'");
        }
        app.get(base, function(req, res) {
          res.setHeader('Content-Type', 'text/html');
          return res.sendfile(main);
        });
      }
      _ref = config.routes.routes;
      for (route in _ref) {
        path = _ref[route];
        route = base + route;
        path = _path.resolve(this.basePath + '/' + path);
        data = {
          route: route,
          path: path
        };
        if (fs.statSync(path).isDirectory()) {
          if (this.v) {
            console.log("Mapping directory '" + path + "' to '" + route + "'");
          }
          app.use(route, express["static"](path));
        } else {
          (function(data) {
            if (_this.v) {
              console.log("Mapping file '" + data.path + "' to '" + data.route + "'");
            }
            return app.get(data.route, function(req, res) {
              res.setHeader('Content-Type', mime.lookup(data.path));
              return res.sendfile(data.path);
            });
          })(data);
        }
      }
      _ref1 = config.packages;
      _fn = function(pckg) {
        if (_this.hasPackageApplication(pckg.name)) {
          if (_this.v) {
            console.log('Mapping file \'' + _path.resolve(pckg.application) + '\' to \'' + base + _path.normalize(pckg.application) + '\'');
          }
          app.get(base + _path.normalize(pckg.application), function(req, res) {
            return _this.buildApplication(pckg.name).then(function(content) {
              res.setHeader('Content-Type', 'application/javascript');
              return res.send(content);
            });
          });
        }
        if (_this.hasPackageStyles(pckg.name)) {
          if (_this.v) {
            console.log('Mapping file \'' + _path.resolve(pckg.style.out) + '\' to \'' + base + _path.normalize(pckg.style.out) + '\'');
          }
          return app.get(base + _path.normalize(pckg.style.out), function(req, res) {
            return _this.buildStyles(pckg.name).then(function(content) {
              res.setHeader('Content-Type', 'text/css');
              return res.send(content);
            });
          });
        }
      };
      for (name in _ref1) {
        pckg = _ref1[name];
        pckg.name = name;
        _fn(pckg);
      }
      app.listen(config.server.port);
      return console.log('Listening on port ' + config.server.port);
    };

    SimQ.prototype.getPackage = function(packageName) {
      var packages;
      packages = this.config.load().packages;
      if (typeof packages[packageName] === 'undefined') {
        throw new Error('Package ' + packageName + ' does not exists');
      }
      return packages[packageName];
    };

    SimQ.prototype.hasPackageStyles = function(packageName) {
      var pckg;
      pckg = this.getPackage(packageName);
      return pckg.style && pckg.style["in"] && pckg.style.out;
    };

    SimQ.prototype.hasPackageApplication = function(packageName) {
      var pckg;
      pckg = this.getPackage(packageName);
      return pckg.application;
    };

    SimQ.prototype.buildStyles = function(packageName) {
      var deferred, pckg,
        _this = this;
      pckg = this.getPackage(packageName);
      deferred = Q.defer();
      if (this.v) {
        console.log("Building styles '" + packageName + "'");
      }
      this.parser.parseStyle(pckg.style["in"], pckg.name).then(function(content) {
        return deferred.resolve(content);
      }, function(e) {
        throw e;
      }).done();
      return deferred.promise;
    };

    SimQ.prototype.buildApplication = function(packageName) {
      var deferred, pckg,
        _this = this;
      pckg = this.getPackage(packageName);
      deferred = Q.defer();
      if (this.v) {
        console.log("Building package '" + packageName + "'");
      }
      this.parser.parseApplication(pckg, pckg.name).then(function(content) {
        return deferred.resolve(content);
      }, function(e) {
        throw e;
      }).done();
      return deferred.promise;
    };

    SimQ.prototype.build = function() {
      var config, name, pckg, _fn, _ref,
        _this = this;
      config = this.config.load();
      _ref = config.packages;
      _fn = function(pckg) {
        if (_this.hasPackageApplication(pckg.name)) {
          _this.buildApplication(pckg.name).then(function(content) {
            return fs.writeFile(_this.basePath + '/' + pckg.application, content);
          });
        }
        if (_this.hasPackageStyles(pckg.name)) {
          return _this.buildStyles(pckg.name).then(function(content) {
            return fs.writeFile(_this.basePath + '/' + pckg.style.out, content);
          });
        }
      };
      for (name in _ref) {
        pckg = _ref[name];
        pckg.name = name;
        _fn(pckg);
      }
      return this;
    };

    SimQ.prototype.watch = function() {
      var ignore, name, pckg, _ref,
        _this = this;
      this.build();
      ignore = new Array;
      _ref = this.config.load().packages;
      for (name in _ref) {
        pckg = _ref[name];
        if (pckg.application) {
          ignore.push(_path.resolve(pckg.application));
        }
        if (pckg.style.out) {
          ignore.push(_path.resolve(pckg.style.out));
        }
      }
      watch.watchTree(this.basePath, {}, function(file, curr, prev) {
        if (typeof file === 'string' && file.match(/~$/) === null && file.match(/^\./) === null && ignore.indexOf(_path.resolve(file)) === -1) {
          if (_this.v) {
            console.log(file);
          }
          _this.config.invalidate();
          return _this.build();
        }
      });
      return this;
    };

    SimQ.create = function(name) {
      var path;
      if (!name) {
        throw new Error('Please enter name of new application.');
      }
      path = _path.resolve(name);
      if (fs.existsSync(path)) {
        throw new Error('Directory with ' + name + ' name is already exists.');
      }
      return ncp.ncp(_path.normalize(__dirname + '/../sandbox'), path, function(err) {
        if (err) {
          throw new Error('There is some error with creating new application.');
        }
      });
    };

    SimQ.prototype.getModuleName = function(path) {
      path = _path.resolve(path);
      path = path.replace(new RegExp('^' + process.cwd() + '\/'), '');
      return path;
    };

    return SimQ;

  })();

  module.exports = SimQ;

}).call(this);
