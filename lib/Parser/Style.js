// Generated by CoffeeScript 1.6.3
(function() {
  var Style, path;

  path = require('path');

  Style = (function() {
    Style.prototype.loader = null;

    Style.prototype.basePath = null;

    Style.prototype.section = null;

    function Style(loader, basePath, section) {
      this.loader = loader;
      this.basePath = basePath;
      this.section = section;
    }

    Style.prototype.parse = function() {
      var dep, dependents, i, _i, _len, _path;
      dependents = this.section.style.dependencies;
      if (dependents.length === 0) {
        dependents = null;
      } else {
        for (i = _i = 0, _len = dependents.length; _i < _len; i = ++_i) {
          dep = dependents[i];
          dependents[i] = path.resolve(this.basePath + '/' + dep);
        }
      }
      _path = path.resolve(this.basePath + '/' + this.section.style["in"]);
      return this.loader.loadFile(_path, dependents);
    };

    return Style;

  })();

  module.exports = Style;

}).call(this);
