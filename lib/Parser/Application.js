// Generated by CoffeeScript 1.6.3
(function() {
  var Application, Helpers, Q, Uglify, path;

  path = require('path');

  Uglify = require('uglify-js');

  Q = require('q');

  Helpers = require('./ApplicationHelpers');

  Application = (function() {
    Application.prototype.simq = null;

    Application.prototype.loader = null;

    Application.prototype.basePath = null;

    Application.prototype.section = null;

    Application.prototype.packageName = null;

    function Application(simq, loader, basePath, section, packageName) {
      this.simq = simq;
      this.loader = loader;
      this.basePath = basePath;
      this.section = section;
      this.packageName = packageName;
    }

    Application.prototype.parseLibraries = function(type) {
      var base;
      base = this.section.base === null ? this.basePath : this.basePath + '/' + this.section.base;
      return Helpers.loadLibraries(this.loader, this.section.libs[type], base);
    };

    Application.prototype.parseModules = function() {
      var base, deferred,
        _this = this;
      deferred = Q.defer();
      base = this.section.base === null ? this.basePath : this.basePath + '/' + this.section.base;
      Helpers.findDependentModulesFromList(this.section.modules, base).then(function(data) {
        return Helpers.loadModules(_this.loader, data.files, _this.section.base).then(function(modules) {
          var alias, module, _ref;
          _ref = _this.section.aliases;
          for (alias in _ref) {
            module = _ref[alias];
            modules.push('\'' + alias + '\': \'' + module + '\'');
          }
          return _this.loader.loadFile(__dirname + '/../Module.js').then(function(content) {
            var info, main, name, node, result, _ref1;
            content = content.replace(/\s+$/, '').replace(/;$/, '');
            base = path.resolve(base);
            node = {};
            _ref1 = data.node;
            for (module in _ref1) {
              info = _ref1[module];
              main = path.relative(base, info.main);
              name = path.relative(base, module);
              main = main.replace(/^[./]+/, '');
              name = name.replace(/^[./]+/, '');
              node[name] = {
                name: info.name,
                path: main
              };
            }
            result = {
              modules: content + '({\n' + modules.join(',\n') + '\n});',
              node: 'require._setNodeInfo(' + JSON.stringify(node) + ');\n'
            };
            return deferred.resolve(result);
          }, function(e) {
            return deferred.reject(e);
          });
        });
      });
      return deferred.promise;
    };

    Application.prototype.parseRun = function() {
      var module, run, _i, _len, _ref;
      run = [];
      _ref = this.section.run;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        module = _ref[_i];
        run.push('this.require(\'' + module + '\');');
      }
      return Q.resolve(run);
    };

    Application.prototype.parse = function() {
      var _this = this;
      return Q.all([this.parseLibraries('begin'), this.parseModules(), this.parseLibraries('end'), this.parseRun()]).then(function(data) {
        var result;
        result = [].concat(data[0], data[1].modules, data[1].node, data[2], data[3]);
        result = result.join('\n\n');
        if (!_this.simq.config.load()["debugger"].scripts) {
          result = Uglify.minify(result, {
            fromString: true
          }).code;
        }
        return Q.resolve(result);
      });
    };

    return Application;

  })();

  module.exports = Application;

}).call(this);
