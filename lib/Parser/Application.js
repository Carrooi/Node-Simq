// Generated by CoffeeScript 1.6.3
(function() {
  var Application, Helpers, Package, Q, Uglify, merge, path;

  path = require('path');

  Uglify = require('uglify-js');

  Q = require('q');

  merge = require('recursive-merge');

  Helpers = require('../Helpers');

  Package = require('../Package');

  Application = (function() {
    Application.prototype.minify = false;

    Application.prototype.loader = null;

    Application.prototype.basePath = null;

    Application.prototype.section = null;

    function Application(loader, basePath, section) {
      this.loader = loader;
      this.basePath = basePath;
      this.section = section;
    }

    Application.prototype.parseLibraries = function(type) {
      return this.loader.loadFiles(this.section.libraries[type]);
    };

    Application.prototype.parseAliases = function() {
      var alias, m, result, _ref;
      result = [];
      _ref = this.section.aliases;
      for (alias in _ref) {
        m = _ref[alias];
        result.push("'" + alias + "': '" + m + "'");
      }
      return result;
    };

    Application.prototype.loadBaseModuleFile = function() {
      var deferred;
      deferred = Q.defer();
      this.loader.loadFile(path.resolve(__dirname + '/../Module.js')).then(function(content) {
        content = content.replace(/\s+$/, '').replace(/;$/, '');
        return deferred.resolve(content);
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    Application.prototype.loadModules = function() {
      var deferred,
        _this = this;
      deferred = Q.defer();
      Package.findDependenciesForModules(this.section.modules).then(function(data) {
        return _this.loader.loadModules(data.files, _this.section.base).then(function(modules) {
          var result;
          modules = modules.concat(_this.parseAliases());
          result = {
            modules: modules.join(',\n'),
            node: Package.parseNodeInfo(data.node, _this.basePath)
          };
          return deferred.resolve(result);
        }, function(err) {
          return deferred.reject(err);
        });
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    Application.prototype.loadFsModules = function() {
      var data, deferred, modules, _path, _ref;
      deferred = Q.defer();
      modules = [];
      _ref = this.section.fsModules;
      for (_path in _ref) {
        data = _ref[_path];
        modules.push(this.loadFsModule(data.name, _path, data.paths));
      }
      Q.all(modules).then(function(data) {
        return deferred.resolve(data);
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    Application.prototype.loadFsModule = function(name, _path, paths) {
      var deferred,
        _this = this;
      deferred = Q.defer();
      Package.findDependenciesForModules(paths).then(function(deps) {
        var file, modules, _i, _len, _name, _ref;
        modules = {};
        _ref = deps.files;
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          file = _ref[_i];
          _name = name + '/' + path.relative(_path, file);
          modules[_name] = file;
        }
        return _this.loader.loadModules(modules, _path).then(function(result) {
          result = {
            modules: result.join(',\n'),
            node: Package.parseNodeInfo(deps.node, _path)
          };
          return deferred.resolve(result);
        }, function(err) {
          return deferred.reject(err);
        });
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    Application.prototype.parseModules = function() {
      var deferred,
        _this = this;
      deferred = Q.defer();
      Q.all([this.loadModules(), this.loadBaseModuleFile(), this.loadFsModules()]).then(function(data) {
        var modules, node, result, sub, _i, _j, _len, _len1, _ref, _ref1;
        modules = [data[0].modules];
        _ref = data[2];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          sub = _ref[_i];
          modules.push(sub.modules);
        }
        modules = modules.join(',\n');
        _ref1 = data[2];
        for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
          sub = _ref1[_j];
          node = merge(data[0].node, sub.node);
        }
        node = JSON.stringify(node);
        result = {
          modules: "" + data[1] + "({\n" + modules + "\n});",
          node: "require._setNodeInfo(" + node + ");\n"
        };
        return deferred.resolve(result);
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    Application.prototype.parseRun = function() {
      var m, match, run, _i, _len, _ref;
      run = [];
      _ref = this.section.run;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        m = _ref[_i];
        if ((match = m.match(/^<(.+)>$/)) === null) {
          run.push("this.require('" + m + "');");
        } else {
          run.push(match[1]);
        }
      }
      return Q.resolve(run);
    };

    Application.prototype.parse = function() {
      var deferred,
        _this = this;
      deferred = Q.defer();
      Q.all([this.parseLibraries('begin'), this.parseModules(), this.parseRun(), this.parseLibraries('end')]).then(function(data) {
        var result;
        result = [].concat(data[0], data[1].modules, data[1].node, data[2], data[3]);
        result = result.join('\n\n');
        if (_this.minify === true) {
          result = Uglify.minify(result, {
            fromString: true
          }).code;
        }
        return deferred.resolve(result);
      }, function(err) {
        return deferred.reject(err);
      });
      return deferred.promise;
    };

    return Application;

  })();

  module.exports = Application;

}).call(this);
